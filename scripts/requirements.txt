# Full dependency set. Used by curate + finalize jobs (which run the
# embedding-dedupe step) and by local development.
#
# For lightweight jobs (scrape / emergency_check / send_newsletter),
# install requirements-light.txt instead — same packages minus
# sentence-transformers + numpy, which together drag in ~2 GB of torch +
# CUDA wheels.

-r requirements-light.txt
sentence-transformers>=5.5.1
numpy>=2.4.6
trafilatura>=2.0.0
