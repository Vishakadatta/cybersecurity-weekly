# Light dependency set: everything *except* torch + sentence-transformers.
# Used by jobs that don't run the embedding-dedupe step:
#   - scrape (RSS only)
#   - emergency_check (LLM clients only)
#   - send_newsletter (Brevo SDK + Jinja only)
#
# Heavy jobs (curate, finalize) install the full requirements.txt instead,
# which adds sentence-transformers and pulls in torch + CUDA wheels (~2 GB).

feedparser>=6.0.12
httpx>=0.28.1
beautifulsoup4>=4.15.0
python-dateutil>=2.9.0
jinja2>=3.1.6
groq>=1.5.0
google-genai>=2.8.0
sib-api-v3-sdk>=7.6.0
